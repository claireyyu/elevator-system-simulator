/**
 * This interface is used to represent a building controller. A building controller can initialize
 * the building elevator system and show the building GUI.
 */
public interface BuildingControllerInterface {
  /**
   * Start the building elevator system.
   */
  void go();
}
